.DS_Store

# Mise-managed project toolchain (Issue #1).
.venv/
.mise/

# Python test/cache artifacts.
.pytest_cache/
__pycache__/
*.py[cod]

# Claude local working artifacts.
.claude/plans/
.claude/agent-memory/
.claude/worktrees/

# SDD runtime artifacts: transient per-run state. Runtime Plan
# (.sdd/plans/) is committed (authoritative per skill); all .sdd/work/
# content (task packages, JSON reports, briefs, run record) is not.
.sdd/work/
