# Stop on max rounds or early stopping signal

Optimization runs end when the maximum number of rounds is reached or when a stopping condition fires, whichever comes first. Stopping conditions cover consecutive non-acceptance, sub-threshold improvement, and budget or runtime caps, so runs terminate on either a hard ceiling or a useful adaptive signal.
