# Optimize artifacts, not flows

MetaCrucible optimizes capability artifacts, not the optimization workflow itself. GBrain's SkillOpt engineering flow is the primary orchestration template and Microsoft SkillOpt is the algorithmic reference, but the first-class optimization targets remain Skills and subagents rather than the workflow machinery that proposes, ranks, applies, and evaluates revisions.
