# Use runtime-native canonical sources

MetaCrucible treats each runtime-native source file as the canonical capability artifact and records MetaCrucible-specific metadata in an artifact envelope. This follows the reference projects' pattern of optimizing Markdown skill documents directly while allowing MetaCrucible to track kind, runtime, path, mutable ranges, and evaluation links without replacing the source format.
